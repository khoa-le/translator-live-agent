# LiveKit server connection
LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# Provider API keys (at least one required)
GOOGLE_API_KEY=
OPENAI_API_KEY=

# Translation settings
SOURCE_LANGUAGE=Japanese
TARGET_LANGUAGE=Vietnamese
TRANSLATION_MODE=realtime_gemini
# TRANSLATION_MODE options:
#   realtime_gemini — Gemini Live API end-to-end (lowest latency, default)
#   realtime_openai — OpenAI Realtime API end-to-end

# Performance profile: "default" or "meeting"
# meeting = faster VAD (0.4s silence), faster endpointing (0.3s), lower AEC warmup
PERFORMANCE_PROFILE=meeting
